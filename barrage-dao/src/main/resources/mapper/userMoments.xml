<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD MAPPER 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hku.barrage.dao.UserMomentsDao">
    <select id="query" parameterType="java.lang.Long" resultType="java.lang.Long">
        select id from t_demo where id = #{id}
    </select>

    <!-- addUserMoments --> 

    <insert id="addUserMoments" parameterType="com.hku.barrage.domain.UserMoment" useGeneratedKeys="true" keyProperty="id">
        insert into
        t_user_moments(
                         userId, `type`, contentId, createTime
        ) VALUES (
                  #{userId}, #{type}, #{contentId}, #{createTime}
                         )
    </insert>
</mapper>